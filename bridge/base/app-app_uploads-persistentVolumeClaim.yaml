#! app-app_uploads-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: app-app-uploads
    namespace: myanmyanlearn
    labels:
        com.docker.compose.service: app
        com.docker.compose.volume: app_uploads
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
